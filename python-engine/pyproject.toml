[project]
name = "pdf-engine"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "pdf2docx>=0.5.13",
    "pymupdf>=1.28.2",
]

[dependency-groups]
dev = [
    "pyinstaller>=6.22.3",
]
